# Initial frozen benchmark run

This commit intentionally changes no retrieval code. It triggers the pre-registered public BEIR workflow for the frozen deterministic retriever.

The resulting SciFact, NFCorpus and ArguAna scores are the initial public baseline and must be preserved before any benchmark-informed tuning.
